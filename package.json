{
  "name": "rezidence4-e2e",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  },
  "scripts": {
    "test:e2e:preflight": "playwright install-deps chromium && playwright install chromium",
    "test:e2e": "playwright test"
  }
}
