/**
 * config/features.js — флаги функциональности
 *
 * Старый realtime-провайдер полностью удалён из проекта.
 * Режим приложения (live/demo) управляется через REACT_APP_RUNTIME_MODE.
 */
export const FEATURES = {};
